<h1><%= @user.name %>さんのページ</h1>
<p>登録メールアドレス: <%= @user.email %></p>
<p>登録タスク: <%= @user.tasks.count %>件</p>

<% if @user.id == current_user.try(:id) %>
<%= link_to %q(タスク一覧へ), tasks_path %>
<% end %>
